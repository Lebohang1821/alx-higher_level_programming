#!/usr/bin/node
// It returns the number of occurrences in a list
exports.nbOccurences = function (list, searchElement) {
  let nOccurrences = 0;
  for (let x = 0; x < list.length; x++) {
    if (searchElement === list[i]) {
      nOccurrences++;
    }
  }
  return nOccurrences;
};
