#!/usr/bin/node

// It prints 3 lines: (like 1-multi_languages.js)
// but by using array of string and loop

const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

let x = 0;

while (x < array.length) {
  console.log(array[x]);
  x++;
}
